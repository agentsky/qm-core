{{- if include "qm.hasSecretEnv" . }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "qm.fullname" . }}-env
  labels:
    {{- include "qm.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.secretEnv }}
  {{- if $v }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
  {{- end }}
{{- end }}
